# Eclipse

Only Buildship is supported.
The legacy `eclipse` task may work but is not guaranteed to work.

## Issues
At the moment if the Kotlin DSL and Kotlin plugin are installed, Eclipse will crash.
The only solution is to uninstall one of the plugins.
